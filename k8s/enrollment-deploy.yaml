apiVersion: apps/v1
kind: Deployment
metadata:
    name: enrollment-svc
    labels:
        app: enrollment-svc
spec:
    replicas: 1
    selector:
        matchLabels:
            app: enrollment-svc
    template:
        metadata:
            labels:
                app: enrollment-svc
        spec:
            containers:
            - name: enrollment-svc
              image: docker.io/<DOCKERHUB_USERNAME>/enrollment-service:latest
              ports:
              - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
    name: enrollment-svc
spec:
    selector:
        app: enrollment-svc
    ports:
    - port: 80
      targetPort: 8080

