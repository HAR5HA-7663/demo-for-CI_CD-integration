apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-svc
  namespace: web-apps
  labels:
    app: user-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-svc
  template:
    metadata:
      labels:
        app: user-svc
    spec:
      containers:
      - name: user-svc
        image: 898919247265.dkr.ecr.us-east-2.amazonaws.com/user-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: user-svc
  namespace: web-apps
spec:
  type: ClusterIP
  selector:
    app: user-svc
  ports:
  - port: 80
    targetPort: 8080

