apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-svc
  namespace: web-apps
  labels:
    app: notification-svc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notification-svc
  template:
    metadata:
      labels:
        app: notification-svc
    spec:
      containers:
      - name: notification-svc
        image: 898919247265.dkr.ecr.us-east-2.amazonaws.com/notification-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: notification-svc
  namespace: web-apps
spec:
  type: ClusterIP
  selector:
    app: notification-svc
  ports:
  - port: 80
    targetPort: 8080

